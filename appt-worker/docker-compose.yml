services:
  appt-worker:
    build: .
    container_name: appt-worker
    env_file: .env
    volumes:
      - /mnt/dispatcher/workers/appt/data:/app/data
    restart: always
    networks:
      - appt-net

networks:
  appt-net:
    external: true
