services:
  channel-order-worker:
    build: .
    container_name: channel-order-worker
    env_file:
      - ../.env.common
      - .env
    ports:
      - "3005:3005"
    restart: always
    networks:
      - appt-net

networks:
  appt-net:
    external: true
